module.exports.wakeThief = (gameId) => {
  // TODO Ask for Thief to wake up
  // TODO Thief can change card
};

module.exports.sleepThief = (gameId) => {
  // TODO Ask for Thief to sleep
};

module.exports.wakeCupid = (gameId) => {
  // TODO Ask for Cupid to wake up
  // TODO Cupid chooses two lovers
};

module.exports.sleepCupid = (gameId) => {
  // TODO Ask for Cupid to sleep
};

module.exports.wakeLovers = (gameId) => {
  // TODO Ask for Lovers to wake up
  // TODO Lovers see each others
};

module.exports.sleepLovers = (gameId) => {
  // TODO Ask for Lovers to sleep
};
